# Copy to /opt/apps/nail-salon/.env on the droplet.
#
# GHCR image coordinates. GH_REPO is "<owner>/<repo>" (must be lowercased).
# The GitHub Actions workflow pushes ghcr.io/<owner>/<repo>:<sha> and :latest.
GH_REPO=minormending/nail-salon
IMAGE_TAG=latest
